name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v1.0.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    name: 🚀 Build Windows Installer & Release
    runs-on: windows-latest
    timeout-minutes: 45

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔧 Verify better-sqlite3 prebuilt binary for Electron 31
        shell: pwsh
        run: |
          Write-Host "Checking better-sqlite3 prebuilt binaries..."
          $bsqlDir = "node_modules\better-sqlite3\build\Release"
          if (Test-Path "$bsqlDir\better_sqlite3.node") {
            $size = [math]::Round((Get-Item "$bsqlDir\better_sqlite3.node").Length / 1KB, 1)
            Write-Host "✓ better_sqlite3.node found ($size KB)"
          } else {
            Write-Host "✗ better_sqlite3.node NOT found — prebuilt binary missing"
            Write-Host "Available files in $bsqlDir :"
            if (Test-Path $bsqlDir) {
              Get-ChildItem $bsqlDir -ErrorAction SilentlyContinue | ForEach-Object { Write-Host "  $($_.Name)" }
            }
            exit 1
          }

      - name: 🔍 Typecheck
        run: npm run typecheck

      - name: 🏗️ Build renderer + main + preload
        run: npm run build

      - name: 🧪 Smoke test
        run: node scripts/smoke-test.mjs

      - name: 📦 Build Windows installer (NSIS)
        run: npm run dist
        env:
          # Code signing disabled in Phase 1 — Phase 5 will add CERT_PASSWORD
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 📊 List release artifacts
        shell: pwsh
        run: |
          Write-Host "### 📦 Release Artifacts" | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
          Write-Host "" | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
          Get-ChildItem -Path release -Recurse -File | ForEach-Object {
            $sizeKb = [math]::Round($_.Length / 1KB, 1)
            Write-Host "- $($_.FullName -replace [regex]::Escape((Get-Location).Path + '\'),'') ($sizeKb KB)" | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
          }
          Get-ChildItem -Path release -Recurse -File | Format-Table Name, Length, FullName

      - name: 📤 Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: ARAY-Setup-${{ github.ref_name || inputs.tag }}
          path: |
            release/ARAY-Setup-*.exe
            release/ARAY-Portable-*.exe
            release/latest.yml
          retention-days: 90
          if-no-files-found: warn

      - name: 🏷️ Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          name: ARAY ${{ github.ref_name }}
          body: |
            # ARAY ${{ github.ref_name }}

            **Are you Ready? and....Yapping!**

            ## 📥 Download

            - **Windows installer**: `ARAY-Setup-*.exe` (below) — recommended
            - **Windows portable**: `ARAY-Portable-*.exe` (below — no install needed)

            ## ⚠️ First-time launch on Windows

            Windows SmartScreen may block unsigned apps. To run ARAY:

            1. Right-click the downloaded `.exe` → **Properties**
            2. Check **Unblock** at the bottom → **Apply**
            3. Double-click to run
            4. If SmartScreen appears: **More info** → **Run anyway**

            ## ✅ Verified

            - ✅ TypeScript typecheck passes
            - ✅ electron-vite build produces main + preload + renderer bundles
            - ✅ 45/45 smoke tests pass
            - ✅ NSIS installer packaged

            ## 📦 What's Inside

            - Electron 31 desktop app
            - React 18 + TypeScript + Vite + Tailwind
            - SQLite database (better-sqlite3)
            - Sharp image processing
            - Local-first storage architecture
            - Camera abstraction (WebcamProvider)
            - Brand design system (Purple Haze × Gold × Silver)

            ## 📚 Documentation

            See [README](https://github.com/${{ github.repository }}/blob/main/README.md) and the `docs/` folder.

            ---

            *Yap. Snap. Repeat.*
          files: |
            release/ARAY-Setup-*.exe
            release/ARAY-Portable-*.exe
            release/latest.yml
          draft: false
          prerelease: false
